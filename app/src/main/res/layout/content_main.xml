<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_main" tools:context=".MainActivity">

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Receive payment"
        android:id="@+id/receivePaymentButton"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:onClick="receivePayment" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Add Product"
        android:id="@+id/addProductButton"
        android:layout_above="@+id/receivePaymentButton"
        android:layout_centerHorizontal="true"
        android:onClick="addProduct"
        android:layout_marginTop="10sp" />

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Transaction History"
        android:id="@+id/transactionHistoryButton"
        android:layout_below="@+id/receivePaymentButton"
        android:layout_centerHorizontal="true"
        android:onClick="transactionHistory" />

    <ImageButton
        android:layout_width="fill_parent"
        android:layout_height="200sp"
        android:id="@+id/balImageButton"
        android:src="@android:drawable/arrow_up_float"
        app:srcCompat="@android:color/holo_green_light"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

</RelativeLayout>
